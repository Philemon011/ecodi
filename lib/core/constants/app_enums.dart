enum LessonStatus {
  notStarted,
  inProgress,
  completed,
}

enum DownloadStatus {
  notDownloaded,
  downloading,
  downloaded,
  error,
}

enum PlayerState {
  idle,
  loading,
  playing,
  paused,
  stopped,
  error,
}